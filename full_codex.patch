The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -6,9 +6,6 @@
 	Maximize2,
 	Power,
 	Play,
-	Pause,
-	Rewind,
-	FastForward,
 	RefreshCw,
 	Square,
 	Target,
@@ -16,12 +16,11 @@
 	Radio,
 	Upload,
 	Cpu,
-	ChevronDown,
-	Loader2,
 } from 'lucide-react';
-import { DemoVideosAPI, IncidentsAPI, JobsAPI, UploadAPI, getAIStreamUrl, WS_BASE_URL, getAuthToken, CamerasAPI } from '../api';
-import { Camera as CameraType, DemoVideo, Incident, JobStatus } from '../types';
+import { DemoVideosAPI, IncidentsAPI, JobsAPI, UploadAPI, getAIStreamUrl, WS_BASE_URL, getAuthToken } from '../api';
+import { DemoVideo, Incident, JobStatus } from '../types';
 import { useLanguage } from '../contexts/LanguageContext';
<truncated 470 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -58,6 +58,7 @@
 }) => {
 	return (
 		<button
+		aria-label={`Play video: ${video.name} - ${video.zone}`}
 			onClick={onClick}
 			className={`group relative overflow-hidden rounded-xl border text-start transition-all ${isActive ? 'border-vs-orange shadow-[0_0_0_1px_rgba(249,115,22,0.45)]' : 'border-zinc-800 hover:border-zinc-700'}`}
 		>
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -75,9 +75,9 @@
 			<div className="absolute inset-0 flex flex-col justify-between p-3">
 				<div className="flex items-start justify-between gap-2">
 					<div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-black/70 px-2 py-1 backdrop-blur-sm">
-						<span className={`h-2 w-2 rounded-full shadow-[0_0_10px_currentColor] ${video.type === 'upload' ? 'bg-cyan-500 text-cyan-500' : 'bg-emerald-500 text-emerald-500'}`} />
+						<span className="h-2 w-2 rounded-full bg-emerald-500 shadow-[0_0_10px_#10b981]" />
 						<span className="font-mono text-[10px] font-bold tracking-[0.2em] text-white">
<truncated 571 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -114,7 +114,7 @@
 const LiveMonitoring = () => {
 	const { t } = useLanguage();
 	const [layout, setLayout] = useState<4 | 9>(4);
-	const [viewMode, setViewMode] = useState<'file' | 'ai'>('ai');
+	const [viewMode, setViewMode] = useState<'file' | 'stream' | 'ai'>('file');
 	const [videos, setVideos] = useState<DemoVideo[]>([]);
 	const [selectedVideoId, setSelectedVideoId] = useState<string>('');
 	const [incidents, setIncidents] = useState<Incident[]>([]);
[diff_block_end]

<truncated 227 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -137,44 +137,6 @@
 	const [aiStreamConnected, setAiStreamConnected] = useState(false);
 	const [uploadBusy, setUploadBusy] = useState(false);
 	const fileInputRef = useRef<HTMLInputElement | null>(null);
-	const videoWrapperRef = useRef<HTMLDivElement>(null);
-	const videoPlayerRef = useRef<HTMLVideoElement>(null);
-	const [isPlaying, setIsPlaying] = useState(true);
-
-	const handlePlayPause = () => {
-		if (videoPlayerRef.current) {
-			if (videoPlayerRef.current.paused) {
-				videoPlayerRef.current.play();
-				setIsPlaying(true);
-			} else {
-				videoPlayerRef.current.pause();
-				setIsPlaying(false);
-			}
-		}
-	};
-
-	const handleSkip = (seconds: number) => {
<truncated 1103 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -178,19 +178,14 @@
 	};
 
 	const startJob = async () => {
-		if (jobBusy) {
-			return;
-		}
-		setJobBusy(true);
-		setJobError(null);
-		try {
-			if (!selectedVideo) {
-				throw new Error("Please select a video first.");
-			}
+		if (!selectedVideo || jobBusy) {
+			return;
+		}
+		setJobBusy(true);
+		setJobError(null);
+		try {
 			const status = await JobsAPI.start(selectedVideo.fileName, 'cam_01');
 			setJobStatus(status);
-			setStreamStarted(true);
-			await loadJobStatus();
 		} catch (error: any) {
 			setJobError(error?.message ?? 'Failed to start worker job');
 		} finally {
[diff_block_end]

<truncated 227 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -200,16 +200,8 @@
 		setJobBusy(true);
 		setJobError(null);
 		try {
-			await JobsAPI.stop();
-			let currentStatus = await JobsAPI.status();
-			let retries = 0;
-			while (currentStatus.running && retries < 10) {
-				await new Promise((resolve) => setTimeout(resolve, 1000));
-				currentStatus = await JobsAPI.status();
-				retries++;
-			}
-			setJobStatus(currentStatus);
-			setStreamStarted(false);
+			const status = await JobsAPI.stop();
+			setJobStatus(status);
 		} catch (error: any) {
 			setJobError(error?.message ?? 'Failed to stop worker job');
 		} finally {
@@ -216,14 +216,6 @@
 		}
 	};
 
-	const handleVideoSelect = async (id: string) => {
<truncated 448 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -276,7 +276,6 @@
 	}, [viewMode, jobStatus.running, jobStatus.cameraId]);
 
 	useEffect(() => {
-		CamerasAPI.getAll().then(setCameras).catch(() => undefined);
 		loadVideos().catch(() => {
 			setIsLoading(false);
 			setDataError('Failed to load demo videos from backend.');
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -309,7 +309,6 @@
 			ws.onopen = () => {
 				reconnectAttemptRef.current = 0;
 				setWsState('connected');
-				setDataError(null);
 			};
 
 			ws.onmessage = (event) => {
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -358,8 +358,8 @@
 
 	return (
 		<div className="flex h-full overflow-hidden bg-[#050505]">
-			<div className="flex-1 flex flex-col min-h-0 overflow-hidden p-2 sm:p-4">
-				<div className="mb-4 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
+			<div className="flex-1 overflow-hidden p-4">
+				<div className="mb-4 flex items-center justify-between gap-4">
 					<div>
 						<h2 className="text-xl font-bold uppercase tracking-wider text-white">{t('liveFeeds')}</h2>
 						<p className="text-[10px] font-mono text-zinc-500">
@@ -366,6 +366,6 @@
 						</p>
 					</div>
<truncated 5976 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -463,69 +463,37 @@
 							<div className="border-b border-red-500/20 bg-red-500/10 px-4 py-2 text-xs text-red-300">{jobError}</div>
 						) : null}
 
-						<div ref={videoWrapperRef} className="relative flex-1 min-h-0 bg-black flex flex-col items-center justify-center">
+						<div className="relative flex-1 min-h-[320px] bg-black">
 							{viewMode === 'ai' ? (
-								<div className="flex h-full w-full flex-col">
-									{/* Video source info bar */}
-									<div className="flex items-center gap-3 border-b border-zinc-800 bg-zinc-950/80 px-4 py-2 backdrop-blur-sm flex-shrink-0">
<truncated 6385 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -520,7 +520,7 @@
 										{selectedVideo?.description ?? 'Select a source to preview it in the dashboard.'}
 									</p>
 									<p className="mt-1 text-[10px] uppercase tracking-[0.2em] text-zinc-600">
-										{viewMode === 'file' ? 'Video endpoint' : 'AI WebSocket Pipeline'}
+										{viewMode === 'file' ? 'Video endpoint' : 'Video feed endpoint'}
 									</p>
 								</div>
 								<div className="flex items-center gap-3 text-zinc-300">
@@ -527,6 +527,6 @@
 										<VolumeX size={18} />
 									</button>
-									<button onClick={toggleFullScreen} className="transition-colors hover:text-white">
<truncated 2838 bytes>
The following changes were made by the USER to: /home/etsh/Documents/علم/graduation project/There is no going back; it's either survival or survival./VisionSafe360/dashboard/components/LiveMonitoring.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -591,10 +591,9 @@
 										<Target size={16} />
 									</div>
 									<div>
-										<p className="text-xs font-semibold text-white">Playback Modes</p>
+										<p className="text-xs font-semibold text-white">How to use this screen</p>
 										<p className="mt-2 text-[11px] leading-relaxed text-zinc-400">
-											<strong className="text-zinc-300">File:</strong> Plays the raw video file natively in the browser.<br />
-											<strong className="text-zinc-300">AI Stream:</strong> Connects to the Edge AI YOLO pipeline. Select an input, click Start, and watch live AI bounding boxes.
<truncated 497 bytes>
